# Развертывание Telegram бота

Подробные инструкции по развертыванию бота на различных платформах для постоянной работы.

## 🚀 Render.com (Бесплатно, Рекомендуется)

### Преимущества
- ✅ **Полностью бесплатно**
- ✅ Автоматическое развертывание из GitHub
- ✅ HTTPS из коробки
- ✅ Простая настройка
- ✅ Логи и мониторинг

### Пошаговая инструкция

#### Шаг 1: Подготовка репозитория

1. **Создайте Git репозиторий** (если еще не создан):
```bash
git init
git add .
git commit -m "Initial commit"
```

2. **Создайте репозиторий на GitHub**:
   - Перейдите на [github.com](https://github.com)
   - Нажмите "New repository"
   - Назовите репозиторий (например, `telegram-channel-bot`)
   - Нажмите "Create repository"

3. **Загрузите код на GitHub**:
```bash
git remote add origin https://github.com/ваш-username/telegram-channel-bot.git
git branch -M main
git push -u origin main
```

#### Шаг 2: Настройка Render.com

1. **Зарегистрируйтесь на Render.com**:
   - Перейдите на [render.com](https://render.com)
   - Нажмите "Get Started for Free"
   - Войдите через GitHub

2. **Создайте новый Web Service**:
   - На dashboard нажмите "New +"
   - Выберите "Web Service"
   - Подключите ваш GitHub репозиторий
   - Нажмите "Connect" рядом с вашим репозиторием

3. **Настройте сервис**:
   - **Name**: `telegram-bot` (или любое имя)
   - **Region**: Выберите ближайший регион
   - **Branch**: `main`
   - **Runtime**: `Docker`
   - **Instance Type**: `Free`

4. **Добавьте переменные окружения**:
   - Прокрутите до раздела "Environment Variables"
   - Нажмите "Add Environment Variable"
   - **Key**: `TELEGRAM_BOT_TOKEN`
   - **Value**: Ваш токен от BotFather
   - Нажмите "Add"

5. **Создайте сервис**:
   - Нажмите "Create Web Service"
   - Render начнет автоматическую сборку и развертывание

#### Шаг 3: Проверка работы

1. **Дождитесь завершения развертывания**:
   - Статус изменится на "Live" (зеленый)
   - Это может занять 2-5 минут

2. **Проверьте логи**:
   - Перейдите на вкладку "Logs"
   - Вы должны увидеть:
   ```
   [SUCCESS] 🤖 Telegram бот успешно запущен!
   [INFO] Ожидание команд...
   ```

3. **Протестируйте бота**:
   - Откройте Telegram
   - Найдите вашего бота
   - Отправьте `/start`
   - Бот должен ответить!

### Автоматическое обновление

После настройки каждый push в GitHub будет автоматически обновлять бота:

```bash
# Внесите изменения в код
git add .
git commit -m "Update bot"
git push
```

Render автоматически пересоберет и перезапустит бота.

### Мониторинг

**Просмотр логов**:
- Dashboard → Ваш сервис → Logs

**Перезапуск бота**:
- Dashboard → Ваш сервис → Manual Deploy → Deploy latest commit

---

## 🚂 Railway.app (Альтернатива)

### Преимущества
- ✅ $5 бесплатных кредитов в месяц
- ✅ Очень простое развертывание
- ✅ Отличный UI
- ✅ Не засыпает

### Инструкция

1. **Зарегистрируйтесь на Railway**:
   - Перейдите на [railway.app](https://railway.app)
   - Войдите через GitHub

2. **Создайте новый проект**:
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Добавьте переменные окружения**:
   - Перейдите в "Variables"
   - Добавьте `TELEGRAM_BOT_TOKEN`

4. **Разверните**:
   - Railway автоматически определит Dockerfile
   - Начнется автоматическое развертывание

---

## 🖥️ VPS (DigitalOcean, AWS, и т.д.)

Если у вас есть свой VPS сервер:

### Установка на Ubuntu/Debian

1. **Подключитесь к серверу**:
```bash
ssh user@your-server-ip
```

2. **Установите Node.js**:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

3. **Установите PM2** (менеджер процессов):
```bash
sudo npm install -g pm2
```

4. **Клонируйте репозиторий**:
```bash
git clone https://github.com/ваш-username/telegram-channel-bot.git
cd telegram-channel-bot
```

5. **Установите зависимости**:
```bash
npm install
```

6. **Создайте .env файл**:
```bash
nano .env
```
Добавьте:
```
TELEGRAM_BOT_TOKEN=ваш_токен
```

7. **Запустите бота с PM2**:
```bash
pm2 start src/bot.js --name telegram-bot
pm2 save
pm2 startup
```

8. **Проверьте статус**:
```bash
pm2 status
pm2 logs telegram-bot
```

### Полезные команды PM2

```bash
# Просмотр логов
pm2 logs telegram-bot

# Перезапуск
pm2 restart telegram-bot

# Остановка
pm2 stop telegram-bot

# Удаление
pm2 delete telegram-bot

# Мониторинг
pm2 monit
```

---

## 🐳 Docker (Универсальный способ)

Если на вашем сервере установлен Docker:

1. **Соберите образ**:
```bash
docker build -t telegram-bot .
```

2. **Запустите контейнер**:
```bash
docker run -d \
  --name telegram-bot \
  --restart unless-stopped \
  -e TELEGRAM_BOT_TOKEN="ваш_токен" \
  telegram-bot
```

3. **Просмотр логов**:
```bash
docker logs -f telegram-bot
```

4. **Остановка**:
```bash
docker stop telegram-bot
```

5. **Перезапуск**:
```bash
docker restart telegram-bot
```

---

## 🔧 Решение проблем

### Бот не запускается на Render

**Проверьте:**
1. Переменная `TELEGRAM_BOT_TOKEN` установлена правильно
2. Dockerfile находится в корне репозитория
3. В логах нет ошибок

### Бот "засыпает" на бесплатном плане

**Решение:**
- Для Telegram ботов с polling это обычно не проблема
- Polling держит соединение активным
- Если всё же засыпает, можно настроить webhook (см. ниже)

### Ошибка "Cannot find module"

**Решение:**
```bash
# Убедитесь, что package-lock.json в репозитории
git add package-lock.json
git commit -m "Add package-lock.json"
git push
```

---

## 🌐 Webhook режим (Опционально)

Для более эффективной работы на production можно использовать webhook вместо polling.

### Преимущества webhook:
- ✅ Меньше нагрузка на сервер
- ✅ Мгновенная доставка сообщений
- ✅ Не засыпает на бесплатных планах

### Недостатки:
- ⚠️ Требует HTTPS (есть на Render/Railway)
- ⚠️ Немного сложнее настройка

> [!NOTE]
> Для начала рекомендую использовать polling режим (текущая настройка). Webhook можно настроить позже при необходимости.

---

## 📊 Мониторинг и логи

### Render.com
- Dashboard → Logs → Real-time logs
- Логи сохраняются 7 дней на бесплатном плане

### Railway.app
- Project → Deployments → Logs
- Real-time логи в удобном интерфейсе

### VPS с PM2
```bash
# Логи в реальном времени
pm2 logs telegram-bot

# Последние 100 строк
pm2 logs telegram-bot --lines 100

# Только ошибки
pm2 logs telegram-bot --err
```

---

## 🔄 Обновление бота

### На Render/Railway (с GitHub)
```bash
git add .
git commit -m "Update bot"
git push
```
Автоматическое развертывание начнется через несколько секунд.

### На VPS
```bash
cd telegram-channel-bot
git pull
npm install
pm2 restart telegram-bot
```

---

## 💡 Советы

1. **Используйте .gitignore**: Никогда не коммитьте `.env` файл
2. **Проверяйте логи**: Регулярно проверяйте логи на наличие ошибок
3. **Тестируйте локально**: Перед push тестируйте изменения локально
4. **Бэкапы**: Регулярно делайте бэкап `data/messages.json`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи на платформе
2. Убедитесь, что токен бота правильный
3. Проверьте, что все файлы в репозитории
4. Попробуйте пересоздать сервис

---

## 🎯 Рекомендация

Для начала используйте **Render.com**:
- Полностью бесплатно
- Простая настройка
- Автоматическое развертывание
- Отличная документация

Если нужна гарантированная работа 24/7 без "засыпания", рассмотрите **Railway.app** ($5 кредитов обычно хватает на месяц для небольшого бота).
